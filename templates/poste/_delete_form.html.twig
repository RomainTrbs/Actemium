<form method="post" action="{{ path('delete_poste', {'id': poste.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer ce poste ? les collaborateurs ayant ce poste n'auront plus de poste défini, pas de retour possible.');" class='d-inline'>
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ poste.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>