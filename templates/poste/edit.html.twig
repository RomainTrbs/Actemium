{% extends 'base.html.twig' %}

{% block title %}Modifier les Informations du Poste{% endblock %}

{% block body %}
    <a href="{{ path('show_poste', {'id': poste.id}) }}"><i class="fas fa-chevron-left h1 float-left"></i></a>
    <h2 class='text-center mb-5 font-weight-bold'>Modifier les Informations du Poste</h2>

    {{ include('poste/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    <center>
        <a href="{{ path('poste_index') }}" class="float mt-2">
            <button class="btn btn-secondary">Retour</button>
        </a>
    </center>
{% endblock %}